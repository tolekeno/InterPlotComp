% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08-plots.R
\name{plot_field_map}
\alias{plot_field_map}
\title{Field-plan heatmap of an observed or fitted quantity.}
\usage{
plot_field_map(
  d,
  value = "Observed",
  title = NULL,
  subtitle = NULL,
  diverging = FALSE,
  facet = FALSE,
  base_size = 12,
  caption = NULL,
  fill_label = NULL
)
}
\arguments{
\item{d}{A data frame with \code{Row}, \code{Column} and the value column. The
coordinates are positions on the field, so they are mapped on continuous
axes; the prepared trial data holds them as factors in \code{Row}/\code{Column} and
as integers in \code{Row_i}/\code{Col_i}, and either form is accepted here.}

\item{value}{Name of the column to map.}

\item{title, subtitle}{Figure text.}

\item{diverging}{Use the diverging palette (for residuals and other signed
quantities) rather than the sequential one (for yields).}

\item{facet}{Facet by environment, for a multi-environment trial.}

\item{base_size}{Base font size in points.}

\item{caption}{Optional caption placed under the figure.}

\item{fill_label}{Legend title; defaults to \code{value}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
The single most useful diagnostic for a spatially analysed trial: it shows
the physical field, so a fertility gradient, a headland effect or a
mis-entered coordinate is visible immediately.
}
\examples{
d <- prepare_trial_data(
  sample_single_trial(),
  list(yield = "Yield_t_ha", geno = "Genotype", row = "Row", column = "Column")
)
plot_field_map(
  data.frame(Row = d$Row_i, Column = d$Col_i, Observed = d$Yield),
  value = "Observed", title = "Observed yield on the field plan"
)
}
