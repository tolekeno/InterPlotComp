% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09-download.R
\name{save_figure}
\alias{save_figure}
\title{Save a ggplot to file at a chosen size and resolution.}
\usage{
save_figure(
  plot_fun,
  file,
  format = "png",
  width = 18,
  height = 12,
  units = "cm",
  dpi = 600
)
}
\arguments{
\item{plot_fun}{A function of one argument, \code{base_size}, returning a
\code{\link[ggplot2:ggplot]{ggplot2::ggplot()}}. Taking a function rather than a finished plot is what
lets the text size follow the export width, so a figure saved at 9 cm and
the same figure at 18 cm both come out legible.}

\item{file}{Destination path. The format is taken from \code{format}, not from
the file extension.}

\item{format}{One of \code{"png"}, \code{"tiff"}, \code{"pdf"}, \code{"svg"} or \code{"eps"}.}

\item{width, height}{Size in \code{units}.}

\item{units}{\code{"cm"} or \code{"in"}.}

\item{dpi}{Resolution for the raster formats; ignored for vector ones.}
}
\value{
The path, invisibly. Called for its side effect of writing the file.
}
\description{
Save a ggplot to file at a chosen size and resolution.
}
\examples{
f <- tempfile(fileext = ".png")
save_figure(
  function(base_size) {
    plot_direct_vs_competition(
      data.frame(Genotype = c("A", "B", "C"),
                 Direct_effect = c(0.5, 0, -0.5),
                 Competition_effect = c(-0.2, 0.1, 0.2),
                 Pure_stand_effect = c(0.1, 0.2, -0.1)),
      base_size = base_size)
  },
  file = f, format = "png", width = 12, height = 9, dpi = 150
)
file.exists(f)
unlink(f)
}
\seealso{
\code{figure_base_size()} for how the text size is chosen.
}
