% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08-plots.R
\name{plot_met_scatter}
\alias{plot_met_scatter}
\title{Direct against competitive effects, one panel per environment.}
\usage{
plot_met_scatter(values, k = 2, base_size = 12, caption = NULL)
}
\arguments{
\item{values}{The genotype-by-environment table from
`fit_met_model()$values`.}

\item{k}{Number of neighbours per plot, from `fit_met_model()$k`.}

\item{base_size}{Base font size in points. [save_figure()] chooses this from
the export width; pass it explicitly only when composing a figure by hand.}

\item{caption}{Optional caption placed under the figure, wrapped to the
plot width.}
}
\value{
A [ggplot2::ggplot()] object.
}
\description{
Direct against competitive effects, one panel per environment.
}
\examples{
values <- expand.grid(Genotype = sprintf("MZ\%02d", 1:8),
                      Environment = paste0("Env0", 1:2),
                      stringsAsFactors = FALSE)
set.seed(3)
values$Direct_effect <- rnorm(nrow(values), 0, 0.5)
values$Competition_effect <- rnorm(nrow(values), 0, 0.2)
values$Pure_stand_effect <- values$Direct_effect + 2 * values$Competition_effect
values$Status <- "Estimable"
plot_met_scatter(values, k = 2)
}
