% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14-run-app.R
\name{run_app}
\alias{run_app}
\title{Launch the inter-plot competition application}
\usage{
run_app(
  launch.browser = interactive(),
  port = NULL,
  host = "127.0.0.1",
  max_upload_mb = 250,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{launch.browser}{Open the application in the default browser.}

\item{port}{Port to listen on. \code{NULL} lets Shiny choose a free one.}

\item{host}{Interface to bind to. The default binds to the loopback address
only, so the application is not exposed to the network.}

\item{max_upload_mb}{Largest file that may be uploaded, in megabytes.}

\item{quiet}{Suppress the start-up report of missing optional packages.}

\item{...}{Further arguments passed to \code{\link[shiny:runApp]{shiny::runApp()}}.}
}
\value{
Called for its side effect of running the application. Does not
return until the application is stopped.
}
\description{
Starts the 'Shiny' interface for fitting direct-competition mixed models to
single-row-plot breeding trials.
}
\details{
Model fitting requires 'ASReml-R', which is commercial software licensed by
VSNi and is not supplied by this package. The interface opens and data can be
inspected without it, so a missing installation is visible immediately rather
than after a long upload; but no model can be fitted until 'ASReml-R' is
installed and licensed in the same R installation that runs the application.
}
\examples{
# Launch the interface (interactive sessions only):
if (interactive()) {
  run_app()
}
}
