% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08-plots.R
\name{plot_stability}
\alias{plot_stability}
\title{Stability of pure-stand performance across environments.}
\usage{
plot_stability(values, top_n = 12, base_size = 12, caption = NULL)
}
\arguments{
\item{values}{The genotype-by-environment table from
\code{fit_met_model()$values}, holding \code{Genotype}, \code{Environment},
\code{Pure_stand_effect} and \code{Status}.}

\item{top_n}{Number of genotypes to trace, taken from the top of the
across-environment mean.}

\item{base_size}{Base font size in points. \code{\link[=save_figure]{save_figure()}} chooses this from
the export width; pass it explicitly only when composing a figure by hand.}

\item{caption}{Optional caption placed under the figure, wrapped to the
plot width.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
A parallel-coordinates view of the selection candidates. Lines that stay
parallel indicate stable genotypes; lines that cross indicate crossover
genotype-by-environment interaction and therefore environment-specific
recommendations.
}
\examples{
values <- expand.grid(Genotype = sprintf("MZ\%02d", 1:6),
                      Environment = paste0("Env0", 1:3),
                      stringsAsFactors = FALSE)
set.seed(2)
values$Pure_stand_effect <- rnorm(nrow(values), 0, 0.4)
values$Status <- "Estimable"
plot_stability(values, top_n = 6)
}
